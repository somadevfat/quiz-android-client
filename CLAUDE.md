日本語ですべて回答

行動する前に必要な情報がない場合ファイルを見る。それでも情報が足りない場合はwebで調べる。
java silverぐらいの知識量への説明
一歩先を考える。しかし勝手に先に行動しない。
記録、検索するときは`date`コマンドで現在時刻を取得する。
# Androidアプリ開発ルール 🧑‍💻

このドキュメントは、本プロジェクトにおけるAndroidアプリ開発の品質、保守性、拡張性を長期的に維持するためのルールを定めたものです。すべての開発者はこのルールを遵守してください。

## 📜 基本原則

- **可読性 (Readability):** 他の人が読んで理解しやすいコードを書きます。複雑なロジックにはコメントを追加します。
- **保守性 (Maintainability):** 将来の機能追加や仕様変更が容易に行えるように、関心事を分離し、疎結合な設計を心がけます。
- **一貫性 (Consistency):** プロジェクト全体でコーディングスタイルや設計パターンを統一します。
- **モダン技術を使う** 最新安定技術を使い安定かつ効率化

---

## 🏗️ アーキテクチャ

本プロジェクトでは、公式に推奨されている **クリーンアーキテクチャ** をベースとした **MVVM (Model-View-ViewModel)** パターンを採用します。

### レイヤー構造

- **UI (Presentation) Layer:**
    - Activity, Fragment, Jetpack Compose, ViewModel を含みます。
    - ユーザーインタラクションの処理と画面描画に責任を持ちます。
    - ビジネスロジックを直接含めず、**Domain Layer** の `UseCase` を呼び出します。
    - UIの状態管理には `StateFlow` または `LiveData` を使用します。

- **Domain Layer:**
    - `UseCase` とドメインモデル（ビジネスオブジェクト）を含みます。
    - アプリケーション固有のビジネスロジックを担当します。
    - **純粋なKotlinモジュール**とし、Androidフレームワークに依存しません。
    - **Data Layer** の `Repository` インターフェースにのみ依存します。

- **Data Layer:**
    - `Repository` の実装、`DataSource` (Remote/Local)、DTO (Data Transfer Object) を含みます。
    - データ取得、保存、キャッシュのロジックを担当します。
    - `Repository` は `UseCase` に対して、データの取得元（APIなのかDBなのか）を隠蔽します。

### 依存関係のルール

依存関係は必ず **UI → Domain → Data** の一方向とします。内側のレイヤー（Domain）は外側のレイヤー（UI, Data）について一切関知しません。

### 依存性の注入 (Dependency Injection)

- **Hilt** を使用して、クラス間の依存関係を注入します。
- `@AndroidEntryPoint`, `@HiltViewModel`, `@Inject` などのアノテーションを適切に使用し、手動でのインスタンス生成を避けます。

---

## ✍️ コーディング規約

コードの一貫性を保つため、以下の規約に従います。

### 言語とスタイル

- **言語:** **Kotlin** を第一言語とします。
- **スタイルガイド:** [Kotlin公式スタイルガイド](https://developer.android.com/kotlin/style-guide) に準拠します。
- **フォーマット:** 静的解析ツール **ktlint** を導入し、CI/CDで自動チェック・修正を行います。

### ベストプラクティス

- **不変性 (Immutability):**
    - 変更の必要がない限り、変数やプロパティは `val` で宣言します。
    - `List`, `Map` などのコレクションも、可能な限り不変（`listOf`, `mapOf`）で扱います。

- **Null安全 (Null Safety):**
    - Null許容型 (`?`) の使用は必要最小限に留めます。
    - Null許容型を扱う際は、`?.let`, `?:` (エルビス演算子) などを活用して安全に処理します。

- **スコープ関数:**
    - `let`, `run`, `with`, `apply`, `also` は、その目的と文脈に応じて適切に使い分けます。可読性を損なう過度なネストは避けてください。

---

## ✨ UI/UX

ユーザーに快適な体験を提供するためのルールです。

- **UI実装:** **Jetpack Compose** の利用を第一選択とします。
    - 再利用可能なコンポーザブル関数を積極的に作成し、UIパーツを共通化します。
    - プレビュー機能 (`@Preview`) を活用し、様々な状態のUIを効率的に確認します。

- **デザインシステム:**
    - **Material Design 3** をベースに、アプリ固有のテーマ（Color, Typography, Shape）を `ui/theme` パッケージで定義し、徹底して利用します。
    - `dp` などの固定値は、可能な限りテーマで定義されたスペーシング定数を使用します。

- **パフォーマンス:**
    - **UIスレッドのブロック禁止:** データベースアクセスやネットワーク通信などの重い処理は、必ず非同期処理で行います。
    - **Composeの最適化:** `remember` を適切に使い、不要な再コンポジションを防ぎます。重い計算は `remember(key) { ... }` や `derivedStateOf` で最適化します。
    - **メモリリーク:** `LeakCanary` をデバッグビルドに導入し、メモリリークを常に監視します。

- **アクセシビリティ:**
    - `Image` や `Icon` には必ず `contentDescription` を設定します。
    - 十分なタップ領域（最低48dp x 48dp）と、テキストと背景のコントラスト比を確保します。

---

## ⚡ 非同期処理

非同期処理は、**Kotlin Coroutines** を標準技術として使用します。

- **CoroutineScope:**
    - ViewModel内では `viewModelScope` を使用します。
    - View（Fragment, Activity）のライフサイクルに紐づく処理では `viewLifecycleOwner.lifecycleScope` を使用します。
    - アプリケーション全体のスコープが必要な場合は、Hiltで `ApplicationScope` を提供します。

- **Dispatcher:**
    - **`Dispatchers.Main`:** UIの更新など、メインスレッドで実行する必要がある処理。
    - **`Dispatchers.IO`:** ネットワーク通信やディスクI/Oなど、ブロッキングが発生する可能性のある処理。
    - **`Dispatchers.Default`:** CPU負荷の高い重い計算（リストのソートやフィルタリングなど）。

- **Flow:**
    - 一度きりの非同期処理は `suspend` 関数を使用します。
    - 時間の経過とともに変化する値やデータのストリームを扱う場合は **Flow** を使用します。
    - UIに状態を公開する場合は、**`StateFlow`** を使用し、ViewModelからのデータフローを構築します。

- **エラーハンドリング:**
    - Coroutine内の例外は `try-catch` で捕捉します。
    - `Flow` のエラーハンドリングには `.catch()` 演算子を使用します。
    - `Repository` 層では、処理結果を `Result` クラスでラップして返すことを推奨します。

---

## 📦 ライブラリ管理

- **バージョン管理:** 依存ライブラリのバージョンは、Gradleの **Version Catalog (`libs.versions.toml`)** を使用して一元管理します。
- **ライブラリ選定:** 新しいライブラリを導入する際は、以下の点を考慮し、チームで合意を得ます。
    - 安定性（Stable版か）
    - メンテナンスの活発度
    - ドキュメントの充実度
    - コミュニティでの評判

---

## ✅ 品質保証

- **テスト:**
    - **ユニットテスト:** ViewModel, UseCase, Repository のロジックを対象とします。（JUnit5, MockK/Mockito）
    - **UIテスト:** Composeの画面とインタラクションを対象とします。（Espresso, Compose Test Rule）
    - テストカバレッジを意識し、主要なロジックは必ずテストで保護します。

- **静的解析:**
    - **ktlint:** コーディングスタイルをチェックします。
    - **detekt:** コードの複雑度や潜在的なバグ（コードスメル）を検出します。
    - これらのツールはCIに組み込み、マージ前に必ず実行します。

- **コードレビュー:**
    - すべてのコードは **Pull Request** を通じてレビューを受けます。
    - レビュワーは1名以上とし、承認（Approve）されるまでマージは行いません。
    - レビューでは、本ドキュメントのルールが守られているかを確認します。

---

##  Git利用ルール

- **ブランチ戦略:** **GitHub Flow** を基本とします。
    - `main`: リリース可能な安定バージョン。
    - `feature/issue-number-description`: 機能開発や修正を行うブランチ。`main`から作成します。

- **コミットメッセージ:** **Conventional Commits** の規約に準拠します。
    - 形式: `<type>(<scope>): <subject>`
    - 例:
        - `feat(login): Add password validation`
        - `fix(profile): Correct user name display`
        - `docs: Update README`
        - `refactor: Simplify user repository`


## 🔒 セキュリティ (新規追加)

このセクションは、アプリケーションとユーザーを脅威から守るためのルールを定めます。開発の初期段階からセキュリティを意識する「**セキュリティ・バイ・デザイン**」を原則とします。

### Androidアプリ

#### データの保護

- **機密情報の保存:**
    - APIキーやユーザーの認証トークンなどの機密情報は、絶対に平文で保存しないでください。
    - **`EncryptedSharedPreferences`** を使用してデータを暗号化します。
    - `SharedPreferences` は、暗号化しない限り、root化された端末から容易に読み取られてしまいます。
- **APIキーの管理:**
    - APIキーやその他のビルド時シークレットは、リポジトリに直接コミットしないでください。
    - **`secrets.properties`** ファイル（`.gitignore` に追加）で管理し、`build.gradle` を通じて `BuildConfig` から参照します。
- **データベースの暗号化:**
    - ユーザーの個人情報など、特に重要なデータをデータベースに保存する場合は、**SQLCipher** などのライブラリを使い、データベースファイル自体を暗号化することを検討します。

#### 通信の安全性

- **HTTPSの強制:**
    - すべてのネットワーク通信は **HTTPS** を使用します。平文での通信 (HTTP) は許可しません。
    - `res/xml/network_security_config.xml` で通信ポリシーを明確に定義し、セキュアな接続のみを許可します。
- **ログ出力の注意:**
    - デバッグ中であっても、個人情報や認証トークンなどの機密情報をログに出力しないでください。
    - 特に、本番（リリース）ビルドでは、デバッグ用のログ出力がすべて無効になっていることを確認します。

#### コンポーネントの安全な公開

- **`android:exported` 属性:**
    - `Activity`, `Service`, `BroadcastReceiver` の `android:exported` 属性は、必要最小限の原則に従って設定します。
    - 他のアプリから呼び出される必要がないコンポーネントは、必ず `android:exported="false"` に設定します。これにより、意図しない外部からの起動や情報漏洩を防ぎます。

#### アプリの保護

- **コードの難読化:**
    - リリースビルドでは、必ず **R8 (ProGuard)** を有効にし、コードの難読化と最適化を行います。これにより、リバースエンジニアリングによるコード解析を困難にします。
- **キーストアの管理:**
    - アプリの署名に使用するキーストアとパスワードは、CI/CD環境や開発者のローカルマシンに直接置かず、安全な場所に厳重に保管します。バージョン管理システムには絶対に含めないでください。

### バックエンド (Spring Boot)

#### 認証・認可

- **フレームワークの活用:**
    - 認証・認可の実装には **Spring Security** を全面的に採用します。自前での実装は脆弱性を生む原因となるため避けてください。
- **パスワード管理:**
    - パスワードは必ずハッシュ化して保存します。**Bcrypt** などの強力でアダプティブなハッシュ関数を使用してください。
- **JWTの利用:**
    - JWT (JSON Web Token) を利用する場合、署名アルゴリズムには **RS256** などの非対称鍵暗号方式を推奨します。
    - トークンには適切な有効期限 (`exp`) を設定し、必要に応じてリフレッシュトークンの仕組みを導入します。

#### 入力値の検証

- **SQLインジェクション対策:**
    - **JPA** (Hibernate) を利用し、動的なクエリを手動で組み立てることは避けてください。これにより、SQLインジェクションの脆弱性を防ぎます。
- **リクエストの検証:**
    - **Bean Validation** (`javax.validation.constraints.*`) を活用し、コントローラー層でリクエストボディやパラメータが期待通りのフォーマット・値であることを必ず検証します。

#### APIとサーバーの保護

- **機密情報の管理:**
    - データベースのパスワードや外部サービスのAPIキーなどの機密情報は、`.properties` や `.yml` ファイルに直接記述せず、**環境変数**や外部のシークレット管理ツールを利用します。
- **エラーレスポンス:**
    - 例外発生時に、スタックトレースや内部のシステム情報など、詳細なエラー情報をクライアントに返さないでください。汎用的なエラーメッセージと、サーバー側で追跡可能なエラーIDを返すように設計します。

### 脆弱性への対応

- **依存ライブラリのスキャン:**
    - **OWASP Dependency-Check** や **Snyk** などのツールをCI/CDに組み込み、利用しているライブラリに既知の脆弱性 (CVE) がないか定期的にスキャンします。
    - 脆弱性が発見された場合は、速やかにライブラリのバージョンアップや代替ライブラリへの移行を検討します。